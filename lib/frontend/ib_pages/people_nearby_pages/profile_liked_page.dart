import 'package:flutter/material.dart';

class ProfileLikedPage extends StatelessWidget {
  const ProfileLikedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
