import 'package:flutter/material.dart';

class IbSingleDatePicker extends StatelessWidget {
  const IbSingleDatePicker({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
